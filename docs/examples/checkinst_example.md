# checkinst

**Description:**

This function demonstrates the usage of the checkinst API.

**Example:**

```lua
-- Check if an instance is valid
local valid = checkinst(some_instance)

if valid then
    print("Instance is valid")
else
    print("Instance is not valid")
end
```
